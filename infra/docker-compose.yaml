version: '3.8'

services:

  app:
    build: ..
    restart: always
    ports:
      - "5000:5000"

  nginx:
    image: nginx:1.19.3
    ports:
      - 80:5000
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always
    depends_on:
      - app
